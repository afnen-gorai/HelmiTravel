import multer from 'multer'
export function notFound(req,res){res.status(404).json({message:'Ressource introuvable'})}
export function errorHandler(err,req,res,next){console.error(`[${new Date().toISOString()}]`,err.message);if(err instanceof multer.MulterError)return res.status(400).json({message:err.code==='LIMIT_FILE_SIZE'?'Image trop volumineuse (5 Mo maximum)':err.message});if(err.name==='SequelizeValidationError'||err.name==='SequelizeUniqueConstraintError')return res.status(422).json({message:'Données invalides',errors:err.errors?.map(e=>({field:e.path,message:e.message}))});if(err.message?.includes('CORS'))return res.status(403).json({message:'Origine non autorisée'});res.status(err.status||500).json({message:process.env.NODE_ENV==='production'?'Une erreur interne est survenue':err.message})}
