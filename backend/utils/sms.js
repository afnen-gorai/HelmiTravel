export async function sendSms({to,message}){if(process.env.SMS_PROVIDER!=='http'||!process.env.SMS_API_URL||!process.env.SMS_API_TOKEN)return {sent:false,disabled:true};const response=await fetch(process.env.SMS_API_URL,{method:'POST',headers:{Authorization:`Bearer ${process.env.SMS_API_TOKEN}`,'Content-Type':'application/json'},body:JSON.stringify({to,message,from:process.env.SMS_SENDER||'HELMI'})});if(!response.ok)throw new Error(`Passerelle SMS indisponible (${response.status})`);return {sent:true,response:await response.json().catch(()=>({}))}}
