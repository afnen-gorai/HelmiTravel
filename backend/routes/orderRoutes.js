import {Router} from 'express';import * as c from '../controllers/orderController.js';import * as gateway from '../controllers/gatewayController.js';import {auth,permit} from '../middleware/auth.js'
const r=Router();r.get('/orders/me',auth,c.mine);r.post('/orders/:id/payment',auth,c.pay);r.post('/payments/paypal/:providerOrderId/capture',auth,gateway.capturePaypal);r.post('/admin/payments/:id/refund',auth,permit('admin'),gateway.refund);r.post('/webhooks/paypal',gateway.paypal);r.get('/admin/orders',auth,permit('admin','agent'),c.all);r.patch('/admin/orders/:id/confirm',auth,permit('admin','agent'),c.confirm);r.get('/orders/:id/confirmation.pdf',auth,c.confirmation);r.get('/order-invoices/:id/pdf',auth,c.invoice);export default r
